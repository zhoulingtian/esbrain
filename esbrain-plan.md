# ESbrain · 西班牙语自学工具规划书

**定位**：零基础自学 → B2，通用西语（非行业专项），可分享给非航空业朋友使用。

**与 FRbrain 的关系**：架构可以复用（单文件 PWA + 子模块 iframe + SRS + 打卡统计），**内容模块必须重新设计**。西语和法语的难点分布完全不同，照搬会同时做多余的功和漏掉真正的坑。

---

## 0. 先说三个最重要的结论

**一、如果 kimi 是以 FRbrain 为蓝本克隆的，它很可能把 FRbrain 的缺陷一起继承了。** 请优先核查三处：复习间隔是不是 `[1,3,7,15,30]` 固定阶梯且 30 天封顶、有没有 ease factor（难度系数）、数据是不是只存浏览器本地没有云备份。这三个是结构性问题，词汇加得越多崩得越快，必须在加内容之前修掉。

**二、西语省下来的力气在发音，多出来的力气全在动词。** 西语拼写几乎是音素级对应（看到就能读），FRbrain 那套 15 条拼读规则（联诵、连音、鼻化元音）在西语里缩到 6 条就够。但西语的动词系统体量约是法语教学负担的两倍——两个过去时的对立、虚拟式的高频实际使用、大量简单过去时不规则形式。资源要从"拼读"搬到"变位"。

**三、必须做地区变体设置，这是 FRbrain 没有的维度。** 西班牙和拉美在人称（vosotros / ustedes）、发音（seseo / ceceo）、词汇（coche / carro / auto）上都有分歧，阿根廷还有 voseo。工具必须让用户在开始时选一个主变体，否则学出来的东西是四不像。你飞整个欧洲的话默认选西班牙本土音，但听力材料要混入拉美口音。

---

## 1. 不要照搬 FRbrain 的部分

### 1.1 发音模块要缩小，重音模块要新建

西语的发音规则只需要覆盖这几条：`c/z` 的 seseo 与 ceceo 差异、`ll/y` 的 yeísmo、`r` 与 `rr` 的单击音与颤音对立、`h` 永远不发音、`g/j` 的 `/x/` 音、`qu/gu` 的不发音 u、`ñ`。六到八条讲完，不需要 FRbrain 那样的 15 条。

**真正需要单独成模块的是重音（acentuación）。** 法语重音固定落在最后一个音节，所以 FRbrain 完全没有这个模块。西语重音位置可变，而且直接改变词义：`hablo`（我说）与 `habló`（他说过）、`canto` 与 `cantó`、`esta` 与 `está`。这是零基础自学者最容易忽略、后期最难纠正的地方。

模块内容应包括：以元音或 n/s 结尾的词重音在倒数第二音节（palabras llanas）、以其他辅音结尾的重音在最后音节（agudas）、违反规则时必须写重音符号、esdrújulas 一定带符号，以及 tilde diacrítica（`si/sí`、`el/él`、`mas/más` 这类靠重音符号区分的同形词）。练习形式建议做成"听音标重音位置"——播放一个词，用户点击重音落在哪个音节。

### 1.2 动词系统要放大

FRbrain 是 30 个动词 8 个时态。西语这个配置不够用。建议目标：**60 个高频动词 × 14 个时态语式**，并且必须包含：

现在时（含 stem-changing 的 e→ie / o→ue / e→i 三类，这是法语没有的规律性变化）、简单过去时（pretérito indefinido）、未完成过去时（imperfecto）、将来时、条件式、现在完成时、过去完成时、虚拟式现在时、虚拟式未完成过去时、命令式（肯定与否定形式不同，否定命令用虚拟式）、动名词与过去分词。

**简单过去时的不规则形式要单独做一个练习池。** `ser/ir`（同形 fui）、`tener`（tuve）、`estar`（estuve）、`hacer`（hice）、`decir`（dije）、`poder`（pude）、`poner`（puse）、`saber`（supe）、`querer`（quise）、`venir`（vine）、`traer`（traje）、`dar`（di）、`ver`（vi）。这批词的高频程度和不规则程度都是最高的，值得脱离普通变位练习单独刷。

### 1.3 三个法语里不存在的专项模块

**ser vs estar** —— 这是西语初学者最大的一道墙，法语只有一个 être，所以 FRbrain 里没有任何对应设计。需要独立模块，讲清永久属性与暂时状态、身份与位置、以及会改变词义的形容词搭配（`ser listo` 聪明 / `estar listo` 准备好了、`ser aburrido` 无聊的人 / `estar aburrido` 感到无聊）。练习必须是二选一的高频快答，目标是形成直觉而不是背规则。

**por vs para** —— 同样没有法语对应。原因与目的、经过与目的地、时长与期限、交换与受益者。建议做成场景化选择题，每题给完整句子上下文。

**pretérito indefinido vs imperfecto** —— 法语的复合过去时与未完成过去时虽然类似，但西语的对立更严格、使用频率更高。需要独立模块讲：已完成的单次动作用 indefinido、背景描述与习惯性动作用 imperfecto、两者在同一句中的配合（`Cuando llegué, ella dormía`）。练习形式建议给一段叙事文本，让用户为每个空选时态，因为这个区别只有在成段语境里才说得清。

### 1.4 阴阳性可以弱化

FRbrain 给词汇做了阴阳性颜色标记，这在法语里非常必要（性别不可预测）。西语约 95% 的名词靠词尾就能判断（`-o` 阳性、`-a/-ción/-dad/-tad` 阴性），所以颜色标记的价值低得多。

建议保留标记但降低权重，把"选冠词"测验改造成**只针对例外词的专项**：`el problema`、`el tema`、`el mapa`、`la mano`、`el agua`（阴性但用 el）、`el día`、以及 `-ista` 结尾的通性词。例外词大约几十个，做成一个小而精的清单比全量测验有效。

### 1.5 地区变体做成设置项

在设置里加一个变体选择，影响三处输出：

人称表上是否显示 `vosotros`（西班牙）或只用 `ustedes`（拉美）；TTS 语音选 `es-ES` 或 `es-MX` / `es-AR`；词汇条目显示哪个变体为主词、哪个为备注。

**建议的默认策略**：主变体选西班牙本土（你飞欧洲，且 DELE 考试和多数教材以此为准），但**听力材料必须混入拉美口音**，比例大约七三。理由是拉美西语的使用人口远大于西班牙，而且听力能力需要覆盖多种口音才算真正的 B2。voseo（阿根廷的 `vos hablás`）只做被动识别，不要求主动产出。

### 1.6 假朋友模块

西语与英语的同形异义词比法语更多也更危险，而且中文母语者往往通过英语中转理解，特别容易踩：`embarazada`（怀孕，不是尴尬）、`éxito`（成功，不是出口）、`actualmente`（目前，不是实际上）、`realizar`（实现/执行，不是意识到）、`asistir`（出席，不是协助）、`sensible`（敏感，不是明智）、`carpeta`（文件夹，不是地毯）、`largo`（长，不是大）。

建议做成独立的小模块，每个词条给出错误理解、正确含义、以及那个被误认的词的真正西语说法。这个模块投入很小但纠错价值很高。

---

## 2. 必须继承的 FRbrain 修复

这几条是 FRbrain 评估里已经定位的问题，如果 kimi 的西语工具是克隆来的，请一并修掉，且**优先于任何内容扩充**。

**复习算法**：间隔上限从 30 天提到 365 天，引入 ease factor 独立追踪每个词的难度。初始 ease 取 2.5，答 easy 时 ease 加 0.15 并按 ease 倍增间隔，答 hard 时 ease 减 0.2（下限 1.3）且间隔只乘 1.2。同时加"暂缓"标记和未来 7/30 天的复习负载预测曲线。

```javascript
// state.reviews 每条记录结构
{ wordId, due, interval, reps, lastReview, ease: 2.5, suspended: false }

function logReview(wordId, level) {
  const rec = state.reviews.find(r => r.wordId === wordId);
  let ease = rec.ease || 2.5;
  let mult;
  if (level === 'easy')      { ease += 0.15; mult = ease; }
  else if (level === 'mid')  { mult = ease; }
  else                      { ease = Math.max(1.3, ease - 0.2); mult = 1.2; }
  rec.ease = ease;
  rec.interval = Math.min(365, Math.ceil((rec.interval || 1) * mult));
  rec.due = fmtDate(addDays(new Date(), rec.interval));
  rec.reps++; rec.lastReview = fmtDate();
  saveState();
}
```

**数据云备份**：本地存储保留为主数据源，增加可选的 GitHub Gist 备份（免费、无需自建后端），记录 lastSync 时间戳做冲突检测，每周日自动存一份学习快照。

**听力音频不要用浏览器 TTS**。`speechSynthesis` 读单词和例句够用，但成段听力需要固定语速和可定位的时间戳（用于重听某一句），浏览器 TTS 给不了。用 Google Cloud TTS 或 ElevenLabs 预生成 MP3 存到 `audio/`，顺带解决离线可用。

---

## 3. 内容规模与分级

| 级别 | 累计词汇 | 课程数 | 核心语法重点 |
|------|---------|-------|------------|
| A1 | 600 | 12 | 现在时规则变位、ser/estar 入门、阴阳性与冠词、gustar 结构 |
| A2 | 1300 | 12 | 两个过去时对立、stem-changing 动词、直接/间接宾语代词、命令式 |
| B1 | 2400 | 24 | 虚拟式现在时、将来与条件、关系从句、现在完成与过去完成 |
| B2 | 3800 | 24 | 虚拟式未完成过去时、条件句三型、被动与 se 结构、语体差异与论述连接 |

词表来源建议用 DELE 官方分级词表或 *Vocabulario en uso*（Edelsa）系列，不要让模型从通用词典批量生成——那样会混入大量该级别用不到的生僻词，稀释复习效率。

每个词条沿用 FRbrain 的字段结构，但增加两个西语专属字段：

```javascript
{
  id: "w0001", word: "hablar", pos: "v",
  gender: null, article: null,
  ipa: "/aˈβlaɾ/",
  stress: 2,              // 重音落在第几个音节（从后往前数）
  variant: null,          // 'es-ES' | 'es-LA' | null（通用）
  altWord: null,          // 变体异称，如 coche 的 altWord 为 "carro/auto"
  zh: "说/讲",
  example: "Hablo español un poco.",
  example_zh: "我会说一点西班牙语。",
  lesson: "A1.1-L01"
}
```

---

## 4. 模块清单

沿用 FRbrain 的屏幕（首页、课程、学习、复习、测验、错题本、词库、统计、周复盘、设置），调整与新增如下：

**改造**：发音模块缩减为 6-8 条规则；"选冠词"测验改为只覆盖例外名词；动词实验室扩到 60 词 × 14 时态。

**新增**：重音训练（听音标重音位置）、ser/estar 快答、por/para 场景选择、两过去时的段落填空、假朋友辨析、成段听力（含拉美口音混入）、发音跟读（`SpeechRecognition` 设 `lang='es-ES'`，不支持时降级为 MediaRecorder 录音回放）、口语任务（角色扮演与两分钟即兴陈述）。

测验类型建议在 FRbrain 四种（西译中、中译西拼写、听音选词、冠词）之外加三种：**变位填空**（给不定式和人称，填出正确形式）、**重音标注**、**时态选择**（indefinido / imperfecto 二选一）。

---

## 5. 给 kimi 的执行轮次

**第一轮（地基，必须先做）**：核查并修复继承来的 SRS 缺陷，加 ease factor 与 365 天上限；加 GitHub Gist 云备份与冲突检测；在设置里加地区变体选项并接通 TTS 语音选择。

**第二轮（西语专属模块）**：新建重音训练模块与规则内容；新建 ser/estar、por/para 两个快答模块；发音规则模块缩减为 6-8 条；把"选冠词"改造为例外名词专项；假朋友模块。

**第三轮（动词系统）**：动词库扩到 60 词，时态扩到 14 个；简单过去时不规则形式独立练习池；stem-changing 三类变化做成分组练习；新增"变位填空"测验类型。

**第四轮（A1-A2 内容打磨）**：词汇补到 1300；课程补齐 24 课；每课加 listening 字段与预生成音频；新增"时态选择"段落填空模块（覆盖两个过去时）。

**第五轮（B1 内容）**：词汇到 2400；新增 B1 的 24 课；虚拟式现在时完整模块；成段听力加入拉美口音材料；发音跟读接 `SpeechRecognition`。

**第六轮（B2 内容与产出）**：词汇到 3800；B2 的 24 课；虚拟式未完成过去时与条件句三型；口语任务模块（角色扮演 + 即兴陈述）；语体差异对照。

**第七轮（辅助）**：学习计划向导（按目标级别与每日可用时间生成每日任务，每周日按完成情况调整）；错题本分类与周报；可选的文化模块（西班牙各自治区 + 拉美主要国家，各自的节庆、饮食、社会习俗）。

---

## 6. 法语基础带来的加速（你个人适用，不影响朋友使用）

你已经在学法语，这对西语是**净优势**，建议在工具里做成一个可关闭的开关。

同源词覆盖率极高，法语与西语的词汇重叠远大于英语与西语，`important/importante`、`nécessaire/necesario`、`développer/desarrollar` 这类几乎可以直接迁移。语法框架也高度相似：阴阳性、冠词、动词按人称变位、虚拟式的存在、代词前置，这些概念你已经建立了，不需要重新理解，只需替换形式。

但有两类干扰要专门标出来。一是**同源假朋友**：法语 `beaucoup` 对应西语 `mucho` 而不存在 `beaucoup` 类似形式；法语 `très` 与西语 `muy` / `mucho` 的分工不同；法语 `en` / `y` 这两个代词西语没有对应物，容易生造。二是**发音回迁**：法语的小舌音会污染西语的 `r`，法语的鼻化元音会让 `-an` / `-on` 读错，法语词尾辅音不发音的习惯会吃掉西语的 `-s` 与 `-n`（而西语词尾 s 承载复数和第二人称，吃掉就是语法错误）。

建议做法：在词条里加一个可选的 `fr` 字段存法语同源词，开启"法语对照"时显示；并在发音模块里加三到五条"法语学习者专属陷阱"提示。这个开关默认关闭，你朋友看不到，不影响通用性。

---

## 7. 时间与验证节奏

七轮全部做完，按模型全职执行估算大约 10 到 14 周。西语的内容量比法语略大（动词系统翻倍），但发音模块省下来一部分，总体接近。

**不建议一口气做完。** 建议节奏是：第一、二、三轮做完就停下来自己用四周——这时你手上是一个 A1 词汇量、但重音、ser/estar、por/para、动词变位全部到位的工具。这四个模块是西语的真正难点，如果这个阶段用得顺，后面加内容只是体量问题；如果不顺，问题会暴露在结构上，那时改比后期改便宜得多。

**一个建议**：西语和法语两条线同时推进，短期内会互相干扰，尤其在发音和相似词汇上。如果你确定两个都要，比较稳的做法是让其中一门先到 A2 稳住（大约建立起基本的语感和发音习惯），再启动第二门，而不是两门都在 A1 阶段平行爬。哪个先不重要，重要的是不要同时在两门的入门期。
